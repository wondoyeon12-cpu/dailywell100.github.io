[build]
  functions = "netlify/functions"
  command = "echo 'No build step required'"

# Netlify Functions는 자동으로 /.netlify/functions/chat로 매핑됩니다
# /api/chat로 접근할 수 있도록 rewrite 설정
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200

